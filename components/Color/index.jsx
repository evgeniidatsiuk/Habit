import {View} from "react-native";

import React from 'react';

export const Color = ({color}) => {
    return (
        <View
            style={{
                borderRadius: 50,
                width: 100,
                height: 100,
                margin: 5,
                backgroundColor: color,
            }}
        />
    )
    r
}
